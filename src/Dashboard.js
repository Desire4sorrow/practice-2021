import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';

export default () => (
    <Card>
        <CardHeader title="Добро пожаловать в Админку базы проектов!" />
        <CardContent>This service is working with React-admin framework.</CardContent>
    </Card>
);